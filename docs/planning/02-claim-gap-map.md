# Nuvia — Claim ↔ Backlog ↔ Reality Map

> The public claims in README.md, the landing page, and docs/technical-specs describe the TARGET state and are preserved verbatim (owner decision). This map shows, for each public claim, the verified current reality and the backlog IDs that close the gap. Citations verified 2026-08-08. **Reality column refreshed 2026-08-08 after Waves A–F landed: every row now reads `holds` — each public claim matches the verified working tree.**
> Convention: "Closing backlog items" lists the DIRECT closers of the row; transitive prerequisites (e.g. C1 for C2) live in each item's Deps line in the backlog and are not repeated here.

| #   | Claim (source)                                                                                                                                                                                                                                                             | Reality today (verified)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Closing backlog items                                                 | Status |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------ |
| 1   | "Five modules connect to a real PostgreSQL database through Drizzle" (README.md:9; landing hero `src/app/page.tsx:162` "Members, events, content, forums, and jobs are live on PostgreSQL today"; specs §1.3 `docs/technical-specs/01-overview.md:20` "Five are `Backed`") | All eleven modules now sit on real PostgreSQL through Drizzle; no mock read path remains (verified: zero `mock-*.ts`/`*MockData*` files in `src/`, no `getMockEvents`/`mockUsers` references). The five claimed modules are backed — members (`src/lib/services/member.service.ts`, `/api/v1/members`), events (`event-read.service.ts`/`event-write.service.ts`, `/api/v1/events` incl. `[id]`; the public page renders real rows), content (`content.service.ts` on the `content` table), forums (`forum.service.ts`), jobs (`job.service.ts`) — and finance, chapters, committees, learning, awards and workspaces are likewise backed (migrations 0000–0009). | B1–B6 (extended to all eleven by the C/D waves)                       | holds  |
| 2   | "Authentication gates access to all five" (README.md:9)                                                                                                                                                                                                                    | Holds: `src/proxy.ts:37-46` gates `/dashboard/**` (matcher `/dashboard/:path*` `src/proxy.ts:127`), `src/lib/dashboard-access.ts:48` `isRoleAllowedForPath` (checks per-section roles from `src/lib/navigation-data.ts`), covered by `tests/dashboard-access.test.ts`.                                                                                                                                                                                                                                                                                                                                                                                            | none (maintain; covered by existing `tests/dashboard-access.test.ts`) | holds  |
| 3   | M3 exit: signup → paid dues → event registration without mock (TODO.md:137 "an association can take a member from signup, to paid dues, to event registration, without touching mock data")                                                                                | Now reachable end-to-end without mock data: member status derives from the subscription lifecycle (A3, `membership-status.service.ts`), the dues/subscription engine runs on `membership_tiers`/`membership_subscriptions` (C2, `subscription.service.ts`), invoices and manual/Stripe payments record into `membership_transactions` (C3, `invoice.service.ts`), and events read/write/registration run on the `events` tables with transactional count integrity (B2/B3). The signup → paid dues → event registration journey touches no mock data.                                                                                                             | A3, B2, B3, C2, C3                                                    | holds  |
| 4   | Module maturity flag planned (README.md:11; ADR-0008 `docs/adr/0008-module-maturity-gate.md:13`)                                                                                                                                                                           | `config/features.ts` now exists with `MODULE_FLAGS` (§13.3 shape) plus `isModuleEnabled`/`getEnabledModules`/`getDisabledModules`/`isMockMarked`/`getModuleForPath`, wired into nav + page gating. All eleven flags are `true`; the Mock tier is empty. Covered by `tests/module-gate.test.ts` (43 tests).                                                                                                                                                                                                                                                                                                                                                        | A1                                                                    | holds  |
| 5   | Promotion order finance → chapters → committees → learning → awards → workspaces (TODO.md:150, :175; specs §13.4 `docs/technical-specs/13-module-maturity-gate.md:40-42`)                                                                                                  | Completed in the declared order: finance (C5), chapters (D1), committees (D2), learning (D3), awards (D4), workspaces (D5) — all promoted 2026-08-08. Every flag in `config/features.ts` is `true`; the §13.4 promotion queue is fully drained and all eleven modules are Promoted.                                                                                                                                                                                                                                                                                                                                                                               | C1–C5, D1–D5                                                          | holds  |
| 6   | Organization singleton exists but unused (TODO.md:139; `src/db/schema/organization.ts`)                                                                                                                                                                                    | Now wired: `organization.service.ts` reads/upserts the single `organizations` row (`getOrganization()`/`updateOrganization()` with cache invalidation) and is consumed across services (settings, branding, currency, email). Landed in A2 (`dc534a3`).                                                                                                                                                                                                                                                                                                                                                                                                           | A2                                                                    | holds  |
| 7   | `media.service.ts` unwired (TODO.md:152)                                                                                                                                                                                                                                   | Rewired to real storage: `media-upload.service.ts` writes uploads to `storage/uploads/` with a JSON manifest (`storage/uploads/manifest.json`), served via `/api/v1/media` + `/api/v1/media/[id]` under `content:*` (B4 sub-decision, `255779a`). The old mock-backed `media.service.ts` was deleted (F2).                                                                                                                                                                                                                                                                                                                                                        | B4 (media sub-decision)                                               | holds  |
| 8   | M4 exit criteria: WCAG 2.2 AA, SLSA provenance, ASVS/SSDF controls mapping, ISO/IEC 27001 A.8 limit, governance (TODO.md:160-162 exit bullets; :165-169 checklist)                                                                                                         | All five M4 checklist bullets are now done: WCAG 2.2 AA gate (E1 — `jsx-a11y` + axe smoke, `docs/accessibility/wcag-2.2-aa-enabled-modules.md`), SLSA Build L2 provenance (E2 — `.github/workflows/release.yml` + `docs/release.md`), OWASP ASVS + NIST SSDF controls mapping (E3 — `docs/security/controls.md`), ISO/IEC 27001 A.8-only statement (E4 — `docs/security/controls.md` ISO section), and governance PR template + CODEOWNERS (E5 — `.github/PULL_REQUEST_TEMPLATE.md`, `.github/CODEOWNERS`).                                                                                                                                                       | E1–E5                                                                 | holds  |
| 9   | Username uniqueness open item (TODO.md:70, "still open from this pass" per TODO.md:68-70 M1 section)                                                                                                                                                                       | Resolved (E6, `45ad555`): the unique constraint is confirmed in the schema (`src/db/schema/users.ts` `username.unique()` + the `drizzle/0000_*` UNIQUE constraint) and the roadmap claim was reconciled to match it; the migration decision for any legacy duplicates is documented. The stale `TODO.md:70` wording is superseded.                                                                                                                                                                                                                                                                                                                                | E6                                                                    | holds  |
| 10  | Audit-surfaced debt: dead `src/lib/config.ts`, mock residue, missing companion docs, nav role-list inconsistency                                                                                                                                                           | All four debts cleared: dead `src/lib/config.ts` deleted (F1, `fd2fafb`); mock residue swept to zero mock files (F2 `0881311` + the D-wave de-mocks); the three companion docs produced (F4 — `docs/api-specs/`, `TASK_BREAKDOWN.md`, `DEPLOYMENT_PLAN.md`, `b9cb6af`); and `superadmin` added consistently to the nav role lists (F3, `3998b75`).                                                                                                                                                                                                                                                                                                                | F1–F4                                                                 | holds  |

## Reading the table

- **Status: gap** means the claim describes a target state that the backlog below exists to make true; **holds** means the claim already matches verified reality.
- Every `file:line` above was re-verified against the working tree on 2026-08-08 before being written. Exact claim wording is quoted from the source document where it matters.
- Backlog ID integrity: each of A1–A3, B1–B6, C1–C5, D1–D5, E1–E6, F1–F4 exists exactly once in `01-todo-backlog.md`; "transitive prerequisites" (Deps) are not repeated here — see the Dependency summary in the backlog.
