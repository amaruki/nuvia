# Nuvia — Claim ↔ Backlog ↔ Reality Map

> The public claims in README.md, the landing page, and docs/technical-specs describe the TARGET state and are preserved verbatim (owner decision). This map shows, for each public claim, the verified current reality and the backlog IDs that close the gap. Citations verified 2026-08-08.
> Convention: "Closing backlog items" lists the DIRECT closers of the row; transitive prerequisites (e.g. C1 for C2) live in each item's Deps line in the backlog and are not repeated here.

| #   | Claim (source)                                                                                                                                                                                                                                                             | Reality today (verified)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Closing backlog items                                                 | Status |
| --- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------ |
| 1   | "Five modules connect to a real PostgreSQL database through Drizzle" (README.md:9; landing hero `src/app/page.tsx:162` "Members, events, content, forums, and jobs are live on PostgreSQL today"; specs §1.3 `docs/technical-specs/01-overview.md:20` "Five are `Backed`") | Only auth/identity/admin touch Drizzle (`src/lib/auth.ts:12`, `src/lib/session-cache.ts:11`, `src/lib/rbac.ts:10`). Module read paths return mocks: `src/lib/services/event.service.ts:99` (`getMockEvents`), `:116` (`getMockEventById`); `src/lib/hooks/use-articles.ts:16` imports `@/lib/data/mock-article-data` (used at `:110`); `src/lib/hooks/use-dues.ts:16` imports `@/lib/data/mock-dues-data` (used at `:36`); public events page hardcodes `events={[]}` (`src/app/(public)/events/page.tsx:36`); `/api/v1/events` has no route handler (`src/app/api/v1/` contains only `admin` and `auth`). | B1–B6                                                                 | gap    |
| 2   | "Authentication gates access to all five" (README.md:9)                                                                                                                                                                                                                    | Holds: `src/proxy.ts:37-46` gates `/dashboard/**` (matcher `/dashboard/:path*` `src/proxy.ts:127`), `src/lib/dashboard-access.ts:48` `isRoleAllowedForPath` (checks per-section roles from `src/lib/navigation-data.ts`), covered by `tests/dashboard-access.test.ts`.                                                                                                                                                                                                                                                                                                                                     | none (maintain; covered by existing `tests/dashboard-access.test.ts`) | holds  |
| 3   | M3 exit: signup → paid dues → event registration without mock (TODO.md:137 "an association can take a member from signup, to paid dues, to event registration, without touching mock data")                                                                                | Not reachable: no dues engine (dues UI renders mock: `src/lib/hooks/use-dues.ts:16`, `:36`) and events read path is mock (`src/lib/services/event.service.ts:99`, `:116`; no `/api/v1/events` routes).                                                                                                                                                                                                                                                                                                                                                                                                     | A3, B2, B3, C2, C3                                                    | gap    |
| 4   | Module maturity flag planned (README.md:11; ADR-0008 `docs/adr/0008-module-maturity-gate.md:13`)                                                                                                                                                                           | `config/features.ts` absent — there is no `config/` directory at all (`ls config` → "No such file or directory"); no `src/lib/features.ts` either.                                                                                                                                                                                                                                                                                                                                                                                                                                                         | A1                                                                    | gap    |
| 5   | Promotion order finance → chapters → committees → learning → awards → workspaces (TODO.md:150, :175; specs §13.4 `docs/technical-specs/13-module-maturity-gate.md:40-42`)                                                                                                  | Not started: every non-foundation module sits at Mock tier with flags false (`docs/technical-specs/13-module-maturity-gate.md:25-37` registry block, `finance: false` `:31` … `workspaces: false` `:36`); no module has been promoted (flag registry `config/features.ts` does not exist).                                                                                                                                                                                                                                                                                                                 | C1–C5, D1–D5                                                          | gap    |
| 6   | Organization singleton exists but unused (TODO.md:139; `src/db/schema/organization.ts`)                                                                                                                                                                                    | Zero importers: `grep -l "schema/organization" src/` matches nothing outside `src/db/schema/index.ts:9` (re-export) and relations; TODO.md:139 "nothing reads or writes it yet".                                                                                                                                                                                                                                                                                                                                                                                                                           | A2                                                                    | gap    |
| 7   | `media.service.ts` unwired (TODO.md:152)                                                                                                                                                                                                                                   | File exists (`src/lib/services/media.service.ts`) and is imported by `src/lib/hooks/use-media.ts:20` (`MediaService`, used at `:111`–`:308`), consumed by three dashboard media pages (`src/app/dashboard/content/media/page.tsx`, `.../media/analytics/page.tsx`, `.../media/edit/[id]/page.tsx`) — but it never writes a file (`storagePath: ''`), so no real storage backend exists. TODO.md:152 "it never writes a file (`storagePath: ''`)".                                                                                                                                                          | B4 (media sub-decision)                                               | gap    |
| 8   | M4 exit criteria: WCAG 2.2 AA, SLSA provenance, ASVS/SSDF controls mapping, ISO/IEC 27001 A.8 limit, governance (TODO.md:160-162 exit bullets; :165-169 checklist)                                                                                                         | Open: the five checklist bullets remain ☐ (TODO.md:165-169); no `jsx-a11y`/`@axe-core/playwright` gate, no SLSA provenance in `docs/release.md`, no PR template/CODEOWNERS. Note: docs/security/controls.md already contains an ASVS/SSDF controls-mapping table with CI-verified vs process-only status (`:11`–`:21`) and an ISO A.8-only statement naming the A.5–A.7 limit + external-auditor requirement (`:5`, `:7`) — so the E3/E4 mappings exist in draft and need completion/verification against the M4 checklist, not creation from scratch.                                                     | E1–E5                                                                 | gap    |
| 9   | Username uniqueness open item (TODO.md:70, "still open from this pass" per TODO.md:68-70 M1 section)                                                                                                                                                                       | Constraint already exists in code — `users.username` is unique: `src/db/schema/users.ts:17` (`username: text("username").notNull().unique()`) and `drizzle/0000_cute_norman_osborn.sql:76` (`CONSTRAINT "users_username_unique" UNIQUE("username")`). TODO.md:70's "not unique in the schema" wording is stale relative to the current schema; the open follow-up is the migration decision for any existing duplicate usernames and reconciling the roadmap claim.                                                                                                                                        | E6                                                                    | gap    |
| 10  | Audit-surfaced debt: dead `src/lib/config.ts`, mock residue, missing companion docs, nav role-list inconsistency                                                                                                                                                           | `src/lib/config.ts` exists (dead parallel to `src/lib/env.ts`); mock residue: `src/lib/mock/eventMockData.ts` (1 file) plus 13 `mock-*.ts` under `src/lib/data/`; `docs/api-specs/`, `docs/TASK_BREAKDOWN.md`, `docs/DEPLOYMENT_PLAN.md` absent (`docs/technical-specs/_index.md:36-38` "not yet produced"); nav role lists omit `superadmin` (`src/lib/navigation-data.ts:82`, `:116`, `:134`, `:161`).                                                                                                                                                                                                   | F1–F4                                                                 | gap    |

## Reading the table

- **Status: gap** means the claim describes a target state that the backlog below exists to make true; **holds** means the claim already matches verified reality.
- Every `file:line` above was re-verified against the working tree on 2026-08-08 before being written. Exact claim wording is quoted from the source document where it matters.
- Backlog ID integrity: each of A1–A3, B1–B6, C1–C5, D1–D5, E1–E6, F1–F4 exists exactly once in `01-todo-backlog.md`; "transitive prerequisites" (Deps) are not repeated here — see the Dependency summary in the backlog.
