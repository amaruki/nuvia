# Security Controls

## Framework Choice

This project uses OWASP ASVS and NIST SSDF (SP 800-218) as its working frameworks. It references ISO/IEC 27001 Annex A.8 (technological controls) only, as a mapping reference, not as a conformance claim (see the ISO statement below). ASVS and SSDF are free, self-assessable, and map to checkable engineering practices. This pairing is realistic for a small OSS project without an audit budget. SLSA build provenance covers the supply-chain and artifact-signing requirements separately (`docs/supply-chain.md`, `docs/release.md`).

## ISO/IEC 27001 Statement (Annex A.8 Only)

**No certification, no conformance claim.** This project is not ISO/IEC 27001 certified, and it does not claim conformance to ISO/IEC 27001. Certification can only be granted by an external, accredited certification body, and no such body has been engaged. No artifact in this repository substitutes for that audit. This holds regardless of what the code does.

**Scope: A.8 technological controls only.** The only ISO/IEC 27001 Annex A controls this document maps are the A.8 technological controls, and it maps them indirectly: the ASVS and SSDF tables below are the working mapping, and this document does not duplicate that coverage. Even within A.8, the mapping is a maintainer self-assessment, not an audit finding.

**A.5–A.7 are out of scope, and a repository cannot satisfy them.** A.5 (organizational controls, such as policies and risk management), A.6 (people controls, such as screening and confidentiality agreements), and A.7 (physical controls, such as facilities) describe an _organization_, not a _codebase_. This project makes no claim over them. An organization that deploys Nuvia and pursues certification must satisfy A.5–A.7 itself, at the organizational level.

**Why this narrow scope.** A claim of broader ISO 27001 coverage than the above would violate the "Transparent" principle in `docs/PRINCIPLES.md`. Listing a framework and letting the reader assume full coverage is exactly the overclaim that principle forbids.

## Control Verification Status

Status vocabulary, per the "Secure" test in `docs/PRINCIPLES.md`. Every row carries exactly one status and names its evidence. **CI-verified** means a job in `.github/workflows/ci.yml` fails when the property regresses; the evidence column names the job and the test file. **Partial CI** means a CI assertion covers part of the control, and the unverified remainder is named. **Process-only** means implemented or documented, but no CI job asserts it; properties that hold "by construction" also carry this status unless a CI job actually asserts the property. **Not started** means unimplemented, tracked in `TODO.md`.

The CI jobs referenced below are the two jobs in `.github/workflows/ci.yml`: `fast` (lint, format, typecheck) and `heavy` (integration tests against real Postgres and Redis, migration check, build, audit). `heavy` runs the whole `tests/` suite through `bun run test:integration`. Both jobs run on every pull request and every push to `main`.

### OWASP ASVS 4.0

| Control area                     | ASVS ref | Status               | Evidence (code → CI verification)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------- | -------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Authentication                   | V2       | Partial CI           | better-auth (`src/lib/auth.ts`). CI `heavy` runs `tests/login-activity.test.ts` (successful and failed sign-ins recorded), `tests/verify-email.test.ts` (no success without a valid signed token), `tests/rate-limit.test.ts` (login bucket returns 429), `tests/profile-update.test.ts`, and `tests/delete-account.test.ts` (password or fresh session required). Not CI-asserted: better-auth's password policy and lockout configuration. Two-factor auth is off (`FEATURE_TWO_FACTOR_AUTH`, `src/lib/env.ts`). |
| Session management               | V3       | Process-only         | Session cookie: 7-day expiry, 1-day refresh, `httpOnly`, `Secure` in production, `SameSite=Lax` (`src/lib/auth.ts:368-379`, ADR-0009). Session-cache endpoints cache only the caller's own session (`TODO.md` M1 hardening pass). No CI job asserts these cookie attributes.                                                                                                                                                                                                                                       |
| Access control                   | V4       | CI-verified          | CI `heavy` runs `tests/auth-route-coverage.test.ts` (every `route.ts` under `src/app/api/v1/**` calls an authorization or session helper; the exception list is empty), `tests/rbac.test.ts` (`requirePermission`, `src/lib/rbac.ts:206`, denies an unauthenticated caller), and `tests/dashboard-access.test.ts` (the `/dashboard/**` role gate, `src/lib/dashboard-access.ts:48`, enforced by `src/proxy.ts`). Caveat: the route-coverage test is a static source-pattern check, not runtime HTTP enforcement.   |
| Validation and sanitization      | V5       | Partial CI           | zod schemas throughout. CI `heavy` runs `tests/profile-update.test.ts` (a whitelist blocks self-assignment of `role` and `emailVerified`), `tests/verify-email.test.ts` (token schema), and `tests/rfc9457.test.ts` (the 422 validation shape, `validationProblem` in `src/lib/http.ts`). Not CI-asserted: every zod schema in every server action and UI form.                                                                                                                                                    |
| Injection prevention             | V5       | Process-only         | Holds by construction: Drizzle's typed query builder emits parameterized queries, and no raw SQL string concatenation exists in the codebase. No CI job asserts that property, so per the "Secure" test this row is process-only. The earlier draft labeled this "CI-verified by construction", which overclaimed.                                                                                                                                                                                                 |
| Cryptographic secret handling    | V6       | CI-verified (narrow) | CI `heavy` runs `tests/env.test.ts` (production boot fails on a placeholder `BETTER_AUTH_SECRET`; `src/lib/env.ts:45-50` requires at least 32 characters) and `tests/seed-script.test.ts` (the seed exits non-zero without a strong `SEED_ADMIN_PASSWORD`, `scripts/seed.ts:23-40`). Password hashing is delegated to better-auth; no CI assertion covers that algorithm choice.                                                                                                                                   |
| Error handling                   | V7       | CI-verified          | RFC 9457 is the sole `/api/v1/**` error contract (`problemResponse`, `src/lib/http.ts:72`). CI `heavy` runs `tests/rfc9457.test.ts`, which asserts the `application/problem+json` members, including a real route's response. Caveat: `AuthResponseFactory` is still used outside `/api/v1/**` (`src/proxy.ts`, the OAuth server action). The earlier draft said "Not started", which was stale.                                                                                                                   |
| Logging and audit trail          | V7       | CI-verified          | CI `heavy` runs `tests/logger.test.ts` (asserts PII redaction, `src/lib/logger.ts:21`) and `tests/change-user-role-transaction.test.ts` (a privileged mutation and its audit entry roll back together, ADR-0009). CI `fast` enforces the oxlint `no-console` error (`.oxlintrc.json`), which keeps the single structured logger (`src/lib/logger.ts`, ADR-0004) the only sink.                                                                                                                                     |
| Data protection                  | V8       | Partial CI           | Erasure is real and tested: CI `heavy` runs `tests/delete-account.test.ts` (the user row is deleted by `src/app/api/v1/auth/delete-account/route.ts`, with a last-superadmin guard). Not implemented: DSAR/self-service export, and a retention schedule (`docs/security/privacy.md`). Log-level PII redaction is covered by the Logging row above. The earlier draft said "DSAR/erasure not implemented"; the erasure half of that was stale.                                                                     |
| Transport and response hardening | V9       | Process-only         | CSP, `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy`, `Permissions-Policy`, and HSTS are set in `next.config.ts:5-31`. No CI job asserts response headers. HTTPS termination is a deployer concern, not a repository one.                                                                                                                                                                                                                                                                           |
| Business logic                   | V11      | CI-verified          | CI `heavy` runs `tests/role-assignment.test.ts` and `tests/custom-roles.test.ts` (`canAssignRole`/`canGrantPermissions`, `src/lib/rbac.ts:337-391`, and the last-superadmin lockout guard) and `tests/rate-limit.test.ts` (429 + `Retry-After` past the threshold, surviving a simulated restart; buckets in `src/lib/rate-limit.ts:31-48`).                                                                                                                                                                       |
| Configuration                    | V14      | CI-verified          | CI `heavy` runs `tests/env.test.ts` (fail-fast env validation at import time, `src/lib/env.ts:104-133`) and `bunx drizzle-kit check` (migration drift). CI installs with `bun install --frozen-lockfile` against exact version pins (`docs/supply-chain.md`).                                                                                                                                                                                                                                                      |

ASVS V10 (Malicious Code) and V13 (API and Web Service) carry no dedicated rows: V10 has no applicable mechanism in this app, and the V13 concerns this app has are covered by the V5 and V7 rows above. V12 (Files and Resources) changed with backlog B4: a live upload path now exists (`src/lib/services/media-upload.service.ts` writes to `storage/uploads/` with a JSON manifest). What is enforced today: a 25 MB size limit (`MAX_UPLOAD_BYTES`), filename truncation to 120 characters, and a sha256 checksum recorded per file. What is not: no malware scanning and no content-type validation of uploaded bytes, and the manifest is file-based, not schema-backed. Those are process-only mitigations until a dedicated V12 row lands.

### NIST SSDF (SP 800-218)

| Practice area                | SSDF ref | Status       | Evidence                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------- | -------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Define security requirements | PO.1     | Process-only | The "Secure" test in `docs/PRINCIPLES.md`, `docs/security/threat-model.md`, and the scope section of `SECURITY.md`. A documentation practice; nothing in CI applies it.                                                                                                                                                                                                |
| Toolchains and automation    | PO.2     | CI-verified  | The control is the pipeline itself: `.github/workflows/ci.yml` runs the `fast` and `heavy` jobs on every pull request and push to `main`. Local `lefthook.yml` hooks mirror it (pre-commit lint/format, pre-push typecheck and tests).                                                                                                                                 |
| Protect the code             | PS.1     | Process-only | Local lefthook hooks, which `--no-verify` bypasses; Conventional Commits plus the `Co-Authored-By` trailer ban (`commitlint.config.ts`, `lefthook.yml`). GitHub branch protection is deferred, an owner decision (`docs/adr/0010`). CI's `--frozen-lockfile` protects the pinned dependency tree.                                                                      |
| Verify releases and attest   | PS.2     | Not started  | SLSA Build Level 2 is the target: `actions/attest-build-provenance` plus cosign keyless OIDC signing (`docs/release.md`, "Artifact signing"). Tracked in `TODO.md` M4.                                                                                                                                                                                                 |
| Analyze first-party code     | PW.2     | Partial CI   | CI `fast` runs `bun run lint`, `format:check`, and `typecheck` (oxlint, oxfmt, `tsc --noEmit`). Enforced human review is absent: branch protection is still deferred (`docs/adr/0010`).                                                                                                                                                                                |
| Analyze third-party code     | PW.3     | Partial CI   | CI `heavy` runs `bun audit --prod`, but non-blocking (`continue-on-error: true` in `ci.yml`). Severity × reachability triage (`docs/supply-chain.md`) with dated, expiring waivers (`SECURITY-WAIVERS.md`). This becomes a hard gate once waivers cover every production high/critical finding. The earlier draft filed this activity under PW.4, which mislabeled it. |
| Reuse secure software        | PW.4     | Process-only | better-auth (sessions, password hashing), Drizzle (parameterized queries), zod (validation). Exact version pins plus `--frozen-lockfile` mean the reviewed dependency tree is the one CI builds.                                                                                                                                                                       |
| Identify vulnerabilities     | RV.1     | Process-only | Private GitHub vulnerability reporting (`SECURITY.md`). `bun audit --prod` runs in CI, non-blocking.                                                                                                                                                                                                                                                                   |
| Triage and remediation       | RV.2     | Process-only | The severity × reachability policy (`docs/supply-chain.md`), with dated, expiring waivers (`SECURITY-WAIVERS.md`). No SLA tooling; `SECURITY.md` states a best-effort acknowledgment.                                                                                                                                                                                  |

### Corrections against the earlier draft

Four rows drifted from reality and were corrected against the code and CI, not quietly rewritten: (1) Authentication said "no CI test yet" — M2's ten tests and the 2026-07-29 hardening pass landed several. (2) Access control said route coverage was not CI-enforced — `tests/auth-route-coverage.test.ts` now enforces it with an empty exception list. (3) Error handling said RFC 9457 was not implemented — it is now the sole `/api/v1/**` contract, tested. (4) Data protection said erasure was not implemented — `delete-account` now really deletes, tested. One row overclaimed: Injection prevention said "CI-verified by construction", but no CI job asserts that property, so it is process-only per the "Secure" test. One row was mislabeled: `bun audit` is third-party code analysis (PW.3), not reuse of secure software (PW.4).

## Update Discipline

Contributors must edit this table in the same PR that changes a control's status. A status in this table that does not match the code is worse than no table.
</content>
