import type { PublicationStatistics } from "@/types/publication";

export interface PublicationsOverviewCardsProps {
  statistics: PublicationStatistics;
}

export interface PublicationStatisticsCardProps {
  statistics: PublicationStatistics;
}
