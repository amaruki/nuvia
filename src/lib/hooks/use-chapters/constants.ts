/** Base route for the chapters API; item routes append `/${id}`. */
export const CHAPTERS_API_PATH = "/api/v1/chapters";

/** Page size the dashboard requests from the chapters list endpoint. */
export const CHAPTERS_PAGE_LIMIT = 100;
