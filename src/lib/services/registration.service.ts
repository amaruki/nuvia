/**
 * Re-export shim — the registration service moved to ./registration/. This
 * file keeps the historical `@/lib/services/registration.service` specifier
 * resolving.
 */

export * from "./registration";
