/**
 * Re-export shim — the subscription service moved to ./subscription/. This
 * file keeps the historical `@/lib/services/subscription.service` specifier
 * resolving.
 */

export * from "./subscription";
